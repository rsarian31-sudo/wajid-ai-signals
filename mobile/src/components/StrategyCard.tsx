import { StyleSheet, Text, View } from "react-native";
import { colors, radius, spacing } from "@/constants/theme";
export function StrategyCard({ title, description, state }: { title: string; description: string; state: string }) { return <View style={styles.card}><View style={styles.copy}><Text style={styles.title}>{title}</Text><Text style={styles.description}>{description}</Text></View><Text style={styles.state}>{state}</Text></View>; }
const styles = StyleSheet.create({ card: { minHeight: 88, flexDirection: "row", alignItems: "center", justifyContent: "space-between", gap: spacing.md, backgroundColor: colors.surface, borderColor: colors.border, borderWidth: 1, borderRadius: radius.md, padding: spacing.md }, copy: { flex: 1 }, title: { color: colors.text, fontSize: 15, fontWeight: "900" }, description: { color: colors.muted, fontSize: 11, marginTop: 5, lineHeight: 16 }, state: { color: colors.warning, fontSize: 9, fontWeight: "900", textTransform: "uppercase" } });
