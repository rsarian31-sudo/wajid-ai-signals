import { StyleSheet, Text, View } from "react-native";
import { colors, radius, spacing } from "@/constants/theme";
export function StatusPill({ label, value, tone = "neutral" }: { label: string; value: string; tone?: "neutral" | "active" | "win" | "loss" }) { const toneColor = tone === "active" ? colors.accent : tone === "win" ? colors.buy : tone === "loss" ? colors.sell : colors.muted; return <View style={styles.pill}><View style={[styles.dot, { backgroundColor: toneColor }]} /><Text style={styles.label}>{label}</Text><Text style={[styles.value, { color: toneColor }]}>{value}</Text></View>; }
const styles = StyleSheet.create({ pill: { alignSelf: "flex-start", flexDirection: "row", alignItems: "center", gap: spacing.sm, paddingVertical: 8, paddingHorizontal: 12, backgroundColor: colors.surface, borderColor: colors.border, borderWidth: 1, borderRadius: radius.pill }, dot: { width: 7, height: 7, borderRadius: 7 }, label: { color: colors.muted, fontSize: 11, fontWeight: "700" }, value: { fontSize: 10, fontWeight: "900" } });
