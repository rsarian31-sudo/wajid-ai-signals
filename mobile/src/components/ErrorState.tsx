import { Pressable, StyleSheet, Text, View } from "react-native";
import { colors, radius, spacing } from "@/constants/theme";
export function ErrorState({ message, onRetry }: { message: string; onRetry: () => void }) { return <View style={styles.container}><Text style={styles.title}>Unable to load data</Text><Text style={styles.message}>{message}</Text><Pressable onPress={onRetry} style={styles.button}><Text style={styles.buttonText}>Retry</Text></Pressable></View>; }
const styles = StyleSheet.create({ container: { padding: spacing.xl, backgroundColor: colors.surface, borderColor: "#5C2D35", borderWidth: 1, borderRadius: radius.md, alignItems: "center" }, title: { color: colors.sell, fontWeight: "900" }, message: { color: colors.muted, fontSize: 11, lineHeight: 17, textAlign: "center", marginTop: spacing.sm }, button: { marginTop: spacing.md, paddingVertical: 9, paddingHorizontal: 14, borderRadius: radius.sm, backgroundColor: colors.surfaceRaised }, buttonText: { color: colors.text, fontSize: 11, fontWeight: "900" } });
